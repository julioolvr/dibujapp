import React from "react";

import { storiesOf } from "@storybook/react";

import SearchPanelsContainer from "./SearchPanelsContainer";

storiesOf("SearchPanelsContainer", module).add("default", () => (
  <SearchPanelsContainer />
));
